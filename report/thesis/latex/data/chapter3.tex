% !Mode:: "TeX:UTF-8"
\chapter{系统设计与实现}

\section{系统目标与关键挑战}

本文的工程目标是构建一个能够自动完成"Verilog代码生成—EDA工具验证—错误反馈—代码修复"闭环的原型系统。给定一道硬件设计任务的自然语言描述和模块接口，系统应当能够调用大语言模型生成候选Verilog代码，借助开源EDA工具自动编译和仿真验证，从验证结果中提取错误信息构造反馈，并驱动模型进行迭代修复，直至代码通过全部测试用例或迭代次数耗尽。

在实现这一目标的过程中，需要解决以下五个工程挑战。

第一，benchmark格式不统一。VerilogEval\cite{verilogeval}和RTLLM\cite{rtllm}在文件组织、题目描述方式和测试平台输出格式上存在差异。如果在实验脚本中分别处理两种格式，后续的生成、验证和反馈逻辑都需要写两套，代码维护成本高且容易出错。

第二，LLM回复格式不可控。大语言模型的回复往往包裹在Markdown代码围栏中，使用CoT提示策略时还会穿插解释性文本。不同提示策略产生的回复结构差异较大，直接保存模型的完整回复无法通过EDA工具编译。

第三，EDA工具的输出需要转化为可比较的分数。编译失败、仿真失败、部分通过和完全通过是性质不同的信号，且VerilogEval与RTLLM的仿真输出格式也不同。系统需要一个统一的评价标准来判断候选代码的质量，并据此选择当前最优候选进入下一轮反馈。

第四，反馈信息的构造需要在信息量和噪声之间取得平衡。将完整的编译日志和仿真波形原样传递给模型会消耗大量上下文窗口并引入噪声，仅告知"编译失败"又缺少足够的修复线索。不同模型对反馈粒度的敏感程度也不同，需要支持多种反馈模式的灵活切换。

第五，实验规模增大后必须保证结果的可复现性。多模型、多条件、多轮实验会产生大量中间产物，如果缺少统一的记录和追溯机制，容易在论文撰写中误用已废弃的结果。项目中多轮对话实验从v1到v2的修正经历就说明了结果追溯的必要性。

下文围绕这五个挑战，依次介绍系统的总体方案和各部分的设计与实现。核心系统代码约2100行，以Python实现，使用Icarus Verilog\cite{iverilog}作为EDA后端。此外还配套实现了RTLLM兼容性扫描、多模式实验调度和结果图表整理等脚本，为后续实验提供工程支撑。

\section{总体方案：EDA反馈驱动的迭代修复闭环}

系统的总体方案是将代码生成任务组织为一个迭代闭环：每轮迭代中，模型根据提示词生成候选代码，EDA工具对候选代码进行编译和仿真，评分器将验证结果映射为标量分数，反馈构造器从验证结果中提取关键错误信息，控制器判断是否需要继续迭代并组装下一轮的提示词。这一闭环的核心假设是：EDA工具能够提供精确的错误定位信息，而大语言模型能够利用这些信息进行定向修复。

为实现上述闭环，系统划分为八个核心模块，各模块的职责和对应代码文件如表\ref{tab:module_map}所示。数据在模块间的流转路径为：任务加载器$\rightarrow$提示词构建器$\rightarrow$LLM客户端$\rightarrow$Verilog提取器$\rightarrow$Verilog执行器$\rightarrow$评分器$\rightarrow$反馈循环控制器$\rightarrow$产物管理器。图\ref{fig:system_architecture}展示了模块间的调用关系。

\begin{table}[htbp]
\centering
\caption{系统核心模块与代码文件映射}
\label{tab:module_map}
\begin{tabular}{lll}
\toprule
模块 & 职责 & 代码文件 \\
\midrule
任务加载器 & 从benchmark目录读取题目 & \texttt{rtllm\_loader.py}, \texttt{verilogeval\_loader.py} \\
提示词构建器 & 组装初始/反馈提示词 & \texttt{prompt\_builder.py} \\
LLM客户端 & 封装API调用与重试 & \texttt{client.py} \\
Verilog提取器 & 从模型回复中提取代码 & \texttt{extract\_verilog.py} \\
Verilog执行器 & 调用iverilog/vvp & \texttt{verilog\_executor.py} \\
评分器 & 编译/仿真结果$\rightarrow$标量分数 & \texttt{ranker.py} \\
反馈循环控制器 & 迭代闭环主逻辑 & \texttt{loop\_runner.py} \\
产物管理器 & 元数据记录与序列化 & \texttt{artifacts.py} \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.92\textwidth]{figure/fig_system_architecture_v2.png}
\caption{系统模块架构与数据流}
\label{fig:system_architecture}
\end{figure}

在工程约束方面，系统需要运行在本科毕设可获取的软硬件环境下：不依赖商业EDA工具许可，不要求GPU本地部署，全部依赖均可通过pip安装。

\section{多源benchmark的统一接入}

针对benchmark格式不统一的问题，系统定义了统一的Task数据结构作为所有下游模块的输入接口。Task包含四个必需字段：\texttt{name}（任务名称，如\texttt{adder\_16bit}）、\texttt{description}（自然语言功能描述）、\texttt{module\_header}（Verilog模块的端口声明）和\texttt{testbench\_path}（测试平台文件的绝对路径）。所有下游模块——提示词构建器、执行器、评分器等——仅依赖Task接口，不感知数据来源是VerilogEval还是RTLLM。

这一设计将benchmark差异封装在加载器内部。RTLLM 2.0的题目分布在Arithmetic、Control、Memory、Miscellaneous四个分类目录下的嵌套子目录中，加载器通过递归遍历目录结构，对每个同时包含\texttt{design\_description.txt}和\texttt{testbench.v}的子目录创建一个Task对象。模块名称从设计描述文件中通过正则表达式提取，提取失败时回退为默认值。VerilogEval采用spec-to-RTL模式，每道题目提供自然语言描述和TopModule接口定义，加载器读取描述文本和接口声明，将参考实现与测试平台合并写入临时文件。两个加载器在输出端均产生统一的Task对象，使后续流程无需区分数据来源。

这种统一接口的好处在实验中得到了体现：当项目从仅支持VerilogEval扩展到同时支持RTLLM时，生成、验证和反馈的代码无需任何修改，只需新增一个加载器即可。

\section{代码生成与提取}

大语言模型的回复格式不可控，这是将LLM接入自动化流水线时必须解决的问题。模型可能返回Markdown代码围栏包裹的代码、穿插在解释文本中的代码片段，甚至在使用CoT策略时先输出一段推理过程再给出代码。如果不做提取处理，这些回复无法直接交给EDA工具编译。

系统通过\texttt{extract\_verilog.py}实现了两阶段代码提取。第一阶段使用正则表达式移除所有Markdown围栏标记（\texttt{```verilog ... ```}），第二阶段通过\texttt{module\textbackslash s+\textbackslash w+...endmodule}模式匹配提取全部Verilog模块块。该策略对不同提示策略产生的回复格式均具有兼容性——无论模型是直接返回代码还是在代码前后添加解释文本，提取器都能正确定位\texttt{module}到\texttt{endmodule}之间的有效代码。

在模型调用层面，系统通过第三方统一API网关接入多种大语言模型。网关使得API密钥和服务端点保持不变，仅通过环境变量中的模型名称参数切换底层模型。这一设计使得在同一实验脚本中切换Haiku、Sonnet和GPT-5.4只需修改一个环境变量。针对API调用中不可避免的瞬态错误（网络超时、服务端过载等），系统实现了指数退避重试机制：对可重试错误依次等待2秒、5秒和10秒，最多重试3次。当所有重试均失败时，错误被记录但不会导致整个实验批次崩溃。

\section{编译仿真与评分}

EDA工具的输出需要转化为可比较的分数，这是实现自动化评估和反馈选择的前提。编译阶段调用Icarus Verilog，命令为\texttt{iverilog -g2012 -o sim.out design.v testbench.v}，其中\texttt{-g2012}选项启用SystemVerilog 2012扩展以支持RTLLM测试平台中使用的\texttt{logic}等新语法。编译超时设为30秒。

编译成功后，使用VVP仿真引擎执行编译产物，超时设为60秒。仿真输出的解析需要支持两种格式：VerilogEval格式使用正则表达式匹配\texttt{mismatched samples is N out of M}模式提取不匹配数和总样本数；RTLLM格式检测\texttt{Your Design Passed}或\texttt{Test completed with N/M failures}。当两种格式均未匹配时，以进程返回码作为最终判断依据。

评分器将上述编译和仿真结果映射为$[-2, 1]$区间内的标量分数（表\ref{tab:rank_score}）。该评分规则借鉴了已有反馈循环研究\cite{autochip}的设计思路：分数越高，代码质量越好。反馈循环控制器以rank$=$1.0作为"通过"的判定阈值。统一的标量评分使得不同来源的候选代码可以直接比较，也为全局最优追踪提供了量化依据。

\begin{table}[htbp]
\centering
\caption{候选代码评分规则}
\label{tab:rank_score}
\begin{tabular}{lrl}
\toprule
情况 & 分数 & 含义 \\
\midrule
未提取到有效Verilog & $-2.0$ & 代码提取失败 \\
编译失败 & $-1.0$ & 存在语法或结构错误 \\
编译有警告但无仿真结果 & $-0.5$ & 可能存在问题 \\
编译成功但无仿真数据 & $0.0$ & 无法评估正确性 \\
仿真部分通过 & $0 \sim 1.0$ & (总样本$-$不匹配)/总样本 \\
仿真完全通过 & $1.0$ & 设计功能正确 \\
\bottomrule
\end{tabular}
\end{table}

需要说明两种中间状态的含义。``编译有警告但无仿真结果''（$-0.5$分）对应编译器报告了warning且仿真进程未能正常启动的情况，通常由使用了Icarus Verilog不完全支持的语法特性引起。``编译成功但无仿真数据''（$0.0$分）则对应编译通过但仿真运行超时或测试平台未产生输出的情况，在RTLLM部分题目的复杂测试平台中偶尔出现。

\section{反馈构造与迭代修复控制}

反馈信息的构造和迭代过程的控制是整个闭环的核心。反馈循环控制器（\texttt{loop\_runner.py}）负责组织"生成—验证—反馈—修复"的迭代流程，其执行逻辑如算法\ref{alg:feedback_loop}所示。

\begin{algorithm}[htbp]
\caption{单轮反馈循环执行流程}
\label{alg:feedback_loop}
\begin{algorithmic}[1]
\REQUIRE 任务 $T$（description, module\_header, testbench\_path）
\REQUIRE 参数 $k$（每轮候选数）、$N$（最大迭代轮数）、$m$（反馈模式）
\ENSURE 最优Verilog代码及其评分
\STATE $\mathit{global\_best} \leftarrow \mathit{null}$, $\mathit{best\_rank} \leftarrow -2.0$
\STATE $\mathit{prompt}_0 \leftarrow \text{BuildInitialPrompt}(T)$
\FOR{$i = 1$ \TO $N$}
    \IF{$i = 1$ \OR $m = \text{retry-only}$}
        \STATE $\mathit{prompt} \leftarrow \mathit{prompt}_0$
    \ELSE
        \STATE $\mathit{prompt} \leftarrow \text{BuildFeedbackPrompt}(T, \mathit{global\_best}, m)$
    \ENDIF
    \FOR{$j = 1$ \TO $k$}
        \STATE $\mathit{code}_j \leftarrow \text{ExtractVerilog}(\text{LLM}(\mathit{prompt}))$
        \STATE $\mathit{rank}_j \leftarrow \text{Evaluate}(\mathit{code}_j, T.\mathit{testbench})$
        \IF{$\mathit{rank}_j > \mathit{best\_rank}$}
            \STATE 更新 $\mathit{global\_best}$, $\mathit{best\_rank}$
        \ENDIF
    \ENDFOR
    \IF{$\mathit{best\_rank} = 1.0$}
        \RETURN $(\mathit{global\_best}, 1.0)$ \COMMENT{设计通过}
    \ENDIF
\ENDFOR
\RETURN $(\mathit{global\_best}, \mathit{best\_rank})$ \COMMENT{迭代耗尽}
\end{algorithmic}
\end{algorithm}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.85\textwidth]{figure/fig_feedback_loop_v2.png}
\caption{反馈循环控制流程}
\label{fig:feedback_loop}
\end{figure}

控制器维护一个跨迭代的全局最优候选。每当某个候选的评分超过当前最优时，控制器更新最优记录。下一轮迭代的反馈提示词始终基于全局最优候选的错误信息构建，确保反馈循环具有单调性：即使某一轮的候选质量暂时回退，已经取得的最优结果也不会丢失。

零样本模式是反馈循环在$k=1$、max\_iterations$=$1时的退化形式。Retry-only模式（$k=3$、max\_iterations$=$5但不注入反馈信息）则在所有迭代中均使用初始提示词。这两种模式与完整反馈模式共享同一代码路径，确保了实验条件的一致性和对比的公平性。当某个候选的API调用失败时，该候选被标记为错误但不终止当前迭代，只有一轮中全部候选都失败时循环才提前退出。

针对反馈信息量与噪声的平衡问题，系统实现了五个梯度的反馈水平（表\ref{tab:feedback_levels}）。从不含任何反馈的L0（零样本）到包含80行仿真输出和分析提示的L4（丰富反馈），这五个级别覆盖了从"完全无信息"到"尽可能多信息"的完整频谱。五个级别共享相同的循环框架，仅在反馈提示词的构造方式上存在差异。L0和L1通过循环控制器的参数控制（单次执行或禁用反馈注入），L2至L4由提示词构建器中的\texttt{FeedbackMode}枚举实现。

\begin{table}[htbp]
\centering
\caption{反馈粒度级别定义}
\label{tab:feedback_levels}
\begin{tabular}{cllp{5.5cm}}
\toprule
级别 & 名称 & 模式 & 反馈内容 \\
\midrule
L0 & Zero-shot & 无反馈 & 仅初始提示词，单次生成 \\
L1 & Retry-only & 无反馈 & 每轮使用初始提示词，多次独立重试 \\
L2 & Compile-only & 编译反馈 & 仅包含编译阶段错误信息 \\
L3 & Succinct & 简洁反馈 & 编译错误+仿真不匹配数+输出前40行 \\
L4 & Rich & 丰富反馈 & 编译错误+仿真输出前80行+分析提示 \\
\bottomrule
\end{tabular}
\end{table}

除单轮API调用模式外，系统还实现了多轮对话反馈。在该模式下，LLM客户端维护一个持续增长的对话历史，每次API调用将完整历史传入，使模型能够看到之前的生成和修复过程。多轮对话的关键约束是反馈消息必须描述模型在当前对话中最近一轮代码的错误，不能引用对话外的代码。该实现在初始版本中曾出现反馈信息与对话上下文不一致的bug，导致实验数据失效；修复后的v2版本严格保证了一致性。

为研究初始提示词对RTL生成质量的影响，系统实现了四种可切换的提示策略\cite{cot}：P0（Base）直接要求模型返回Verilog代码，P1（CoT）要求先推理再输出代码，P2（Few-shot）在提示词中附加两个示例，P3（Few-shot+CoT）结合示例和推理。示例选取避免了与正式实验任务重叠，两个示例均不在RTLLM\_STUDY\_12中。策略通过\texttt{PromptStrategy}枚举定义，策略名称自动记录到实验元数据中。

\section{实验配置扩展与结果管理}

实验规模增大后，结果的可复现性和可追溯性变得尤为重要。每次实验运行被分配一个唯一的\texttt{run\_id}（如\texttt{rtllm\_feedback\_20260420\_153022}），产物存放在\texttt{outputs/runs/<类别>/<run\_id>/}下，包含三类文件：\texttt{summary.json}记录运行级汇总，\texttt{details.json}记录每道题每轮每个候选的完整信息，\texttt{summary.csv}提供表格化的题目级结果。

产物管理器在每次运行开始时自动采集元数据，包括当前Git commit hash、分支名称、工作区是否有未提交修改、Python版本、操作系统平台和完整的命令行参数。这些信息使得任何实验结果都可以追溯到产生它的代码版本和运行环境。

项目设计了两个审计脚本来保障数据可信度。代码审计脚本（\texttt{audit\_project.py}）执行24项自动化检查，涵盖项目结构、依赖完整性和代码风格。数据审计脚本（\texttt{audit\_data.py}）遍历全部实验运行目录，从原始\texttt{summary.json}中重新计算通过率并与文档记录的结论交叉验证。此外，项目维护一份实验结果索引文档，对每个实验运行标注其状态（有效/已废弃/已替代），避免论文撰写中误引用已废弃的结果。本文将各组实验的主要结果整理为通过率对比图、逐题矩阵和稳定性误差棒图等图表，供第5章结果分析引用。

\section{本章小结}

本章围绕"让LLM自动生成并修复Verilog代码"这一目标，分析了benchmark格式不统一、LLM回复格式不可控、EDA输出需转化为可比较分数、反馈信息量与噪声的平衡、以及实验可复现性五个工程挑战，并逐一介绍了对应的设计方案和实现。系统通过统一的Task接口屏蔽了两种benchmark的格式差异，通过两阶段提取策略处理了LLM的多样化回复格式，通过$[-2,1]$标量评分实现了编译仿真结果的统一量化，通过五级反馈粒度和多轮对话两种机制为反馈构造提供了灵活配置，通过元数据记录和审计脚本确保了实验结果的可追溯性。这些模块共同构成了第4章实验设计和第5章结果分析的工程基础。
